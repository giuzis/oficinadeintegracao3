import 'package:flutter/material.dart';

// QR Code page
class QRCodePage extends StatelessWidget {
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text('Abra-te câmera!', style: TextStyle(fontSize: 30.0)),
      ),
    );
  }
}
